<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProfile name="dev,local">
        <!-- Development: Human-readable console output -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>

        <logger name="ua.cn.stu.npp" level="DEBUG"/>
        <logger name="org.springframework.web" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <!-- Production: JSON format to stdout -->
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>true</includeContext>
                <includeMdc>true</includeMdc>
                <includeStructuredArguments>true</includeStructuredArguments>
                <includeTags>true</includeTags>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
                <customFields>{"application":"npp-portal-backend","environment":"${SPRING_PROFILE:-production}"}</customFields>
            </encoder>
        </appender>

        <root level="${LOGGING_LEVEL:-INFO}">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>

        <logger name="ua.cn.stu.npp" level="${LOGGING_LEVEL:-INFO}"/>
    </springProfile>
</configuration>